using UnityEngine;
using System.Collections;

public class WhileActiveAlsoEnable : MonoBehaviour {
	public Behaviour[] targets;

/*	void OnEnable() {
		foreach(var target in targets) {
			//target.enabled = true;
        
		}
	}
	void OnDisable() {
		foreach(var target in targets) {
			//target.enabled = false;
		}
	}
*/
}

